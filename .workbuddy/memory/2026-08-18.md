# 微信读书 · 设计/数据工作日志 (2026-08-18)

## yao-weread-skill 可视化报告
- 项目级安装了 yao-weread-skill（.workbuddy/skills/），安全审计 P2 安全。
- 运行失败根因：脚本 `generate_weread_report.py` 写死 `SKILL_VERSION="1.0.3"`，微信读书网关对 `/shelf/sync` 等返回 upgrade_info 后硬报错（raise）。官方 weread skill 为 1.0.4，可通过网关。
- 修复：把 yao 脚本 SKILL_VERSION 改为 "1.0.4"，真实报告即可运行（环境已有 WEREAD_API_KEY）。
- 真实报告产出：`reports/generated/real-report/weread-report.html`（26 个 ECharts 图，含用户真实读书数据，34s 生成）。
- 样例模式：`--sample-ai-founder --sample-scale 5` 可绕过网关验证可视化质量（无需 key）。
- 封面接口：`/book/info?bookId=XXX` 返回 cover（cdn URL），比 shelf 更全，是可靠的逐本封面来源。

## 金句卡片 · 翻转卡片（flip card）
- 需求：正面名言、背面书籍封面；在线引用微信读书封面 URL；暂不管打印。
- 数据桥接：金句库 bookId 与 weread_shelf.json 书名对不齐（shelf 存全集/影视原著等长标题，仅 9/83 精确匹配），改用 bookId 经 `/book/info` 逐本取封面。cards.html 的 351 个 `<article>` 顺序与金句库 selected 顺序一致 → 封面按 index 对齐注入（351/351 命中）。
- 实现：CSS 3D flip（perspective 在 .card；.card-inner height:100%+preserve-3d；.card-front/.card-back backface-visibility:hidden；.card-back absolute inset:0 预旋180）。原 .card-inner 的 flex/padding/position:relative 平移到 .card-front，正面外观不变。
- 质量审查 24/25，REVISE（P1：键盘可达 + reduced-motion）→ 已修：351 卡片加 tabindex=0、keydown(Enter/Space)、:focus-visible、@media reduced-motion；0 个 http:// 混内容。
- 重新打包 `cards_package.zip`（≈545KB，design-tokens.md 从父目录取，使用说明追加「翻转卡片」一节）。
- 临时构建文件（_title2cover*.json、_cover_by_index.json、_fetch_covers.py）已清理；.bak 版本备份保留。
