# 2026-09-14 工作日志

## neat-freak 收尾（微信读书项目）
- 走轻量路径五步。根目录当前 4 文件夹（data/ quote_lib/ reports/ scripts/）+ 隐藏 .workbuddy/，无 README/规则文件、非 git。
- 对齐事实：4 个 scripts 数据路径已真实指向 `data/`（上次整理改动落地，无断链）。
  - 偏差1（pending）：`analysis.py` 依赖 `plotly`+`wordcloud`，managed venv 未装，重跑会 ImportError。
  - 偏差2（minor）：`build_quote_lib.py` 写死绝对路径 `D:/workbuddy/微信读书/...`，能跑但不便携。
- 补规则文件：新建根 `AGENTS.md`（≤60行，定位/怎么跑/技术栈/目录约定/状态下一步），作为下次会话恢复入口。
- 全项目扫漏网：无 `__pycache__`/`*.out`/`*.log`/`*.tmp`/`nul` 残留。
- 删除候选（已确认）：
  1. `.workbuddy/backup/ai-founder-sample_20260906/`（328K，已删样例报告的备份，09-06 留的安全网）
  2. `.workbuddy/backup/weread-skill-1.0.0/skill/`（11文件，旧 skill v1.0.0 备份，已被 yao-weread-skill v1.0.4 取代）
  3. `quote_lib/.backups/` 5 个 cards.html.bak_*（翻转卡迭代历史副本，已隐藏）
- **清场结果（用户确认「3 个候选全删」）**：3 项已全部 `rm` 删除；`backup/` 与 `quote_lib/.backups/` 现均为空；全项目扫描无 `__pycache__`/`*.out`/`*.log`/`*.tmp`/`nul` 残留。根目录终态：`AGENTS.md` + `data/` `quote_lib/` `reports/` `scripts/`。
- 现役保留：`.workbuddy/skills/{yao-weread-skill, huashu-weread}` 两个技能；`data/analysis/reading_dashboard.html`+csv（分析产物，可重生成）。
