# 2026-08-12 微信读书 Skill 接入日志

## 配置与升级
- 微信读书官方 Skill 已安装（weread-skills-official），API Key 已配置为**长期生效**：写入 Windows 用户级环境变量 `WEREAD_API_KEY` + `~/.bashrc` export。
- 网关 `https://i.weread.qq.com/api/agent/gateway` 连通验证通过（`/_list` 返回 18 个接口，`errcode:0`）。
- 首次 `/shelf/sync` 回包携带 `upgrade_info`（latest 1.0.4 / current 1.0.0），按 Skill 硬规则**先升级再回答**：从 `https://cdn.weread.qq.com/skills/weread-skills.zip` 下载覆盖，旧版备份至 `.workbuddy/backup/weread-skill-1.0.0/skill`，`_skillhub_meta.json` 版本号更新为 1.0.4。升级后重跑 `upgrade_info` 消失。

## 书架数据（v1.0.4 实测）
- 可见条目总数 **499**：电子书 498 + 文章收藏 1，有声书/专辑 0。
- 公开阅读 477 / 私密阅读 22；已读完 3 本。
- 分类 Top：精品小说-悬疑推理 96、精品小说-科幻小说 91、未分类 21、科学科普 20、经典作品 18 等。
- 最近阅读（2026-08）：去你的岛、自杀的心理学家(索析)、现象学与心理学(福柯)、赶时间的人(王计兵)、在峡江的转弯处(陈行甲,✓) 等。
- 原始 JSON 快照：`D:/workbuddy/微信读书/weread_shelf.json`（192KB，可复查）。

## 全量笔记/划线导出（含踩坑修复）
- 脚本：`D:/workbuddy/微信读书/export_notes.py`（拉 /user/notebooks → 逐本 /book/bookmarklist + /review/list/mine，含重试/进度日志/每20本落盘）。
- **关键坑**：网关 `/review/list/mine` 默认 `count` 上限 100 且 `hasMore` 标记不可靠；首次用 `count=20` 导致所有「想法>20 条」的书被截断（想法仅 774）。验证确认 `count=500` 可一次拿全，改后重跑 → 想法 **1305** 条，与服务端 totalNoteCount(7415) 吻合（差3，正常边界）。
- 最终产出：`weread_notes_export.json`（3.13MB，结构化）+ `weread_notes_export.md`（2.61MB，按书/章节分组：划线用 `>` 引用、想法附原文、书签只计数）。
- 统计：278 本书 · 划线 6099 · 想法/点评 1305 · 书签计数 8 · 可导出内容合计 7404。
- Top 内容最多：在峡江的转弯处(586)、死人经(507)、我是个算命先生(467)、明朝那些事儿(400)、罪与罚(299) 等。

## 注意事项
- 每次请求必须带 `skill_version`（当前 1.0.4）且参数平铺在 body 顶层。
- 书架数量口径：`books.length + albums.length + (mp非空?1:0)`，专辑/有声书必须计入。
- 调用任何接口前需先读对应说明文件（search/book/shelf/readdata/notes/review/discover）。
- **导出 review/list/mine 必须把 count 设大（≥500）**，否则想法被静默截断，且不能用 hasMore 判断到底。

## 阅读行为分析（进行中）
- 用户决策：全量 16 项分析（A 偏好画像 / B 时间节律 / C 内容NLP / D 章节结构 / E 交叉高级），交付单页 HTML 交互看板（plotly），不做心理学专业专题。
- 数据增强抓取 `fetch_enrich.py`（后台）：`/readdata/detail`（overall + 逐年2023-2026 + 逐月2023-01~2026-08 共49次，拿**真实阅读秒数/天数/偏好分类/24h时段/偏好作者出版社**）+ 278本 `/book/getprogress`（真实进度%+阅读秒数）+ 278本 `/book/info`（出版年/字数/出版社/评分）。产出 `weread_readdata.json` / `weread_progress.json` / `weread_bookinfo.json`。落盘到 `D:/workbuddy/微信读书/`。
- B 类时间节律改用 `/readdata/detail` 真实阅读秒数（替代笔记 createTime 代理），并用 `preferCategory` 交叉验证 A 类。
- 分析脚本 `analysis.py`（已写+语法通过）：读 5 个 JSON，plotly 生成 `analysis/reading_dashboard.html` + 各 CSV（A1/A2/C8/C10/B5 等）。用 managed venv（已装 jieba+plotly）运行：`$VENV/Scripts/python.exe analysis.py`。
- 技术栈：managed python 3.13 venv 路径 `C:/Users/Administrator/.workbuddy/binaries/python/envs/default`，jieba 做中文分词/词云候选，plotly 自包含 HTML（首个图 embed plotly.js，其余 False）。
- NLP 简化：情感极性用语词典法（POS/NEG 词表），非模型；关键词用 jieba + 停用词表。

## 分析看板已交付 ✅
- 增强抓取出 3 文件：`weread_readdata.json`(49次readdata: overall+逐年2023-26+逐月44) / `weread_progress.json`(278本) / `weread_bookinfo.json`(278本)。抓取 12:36–12:39 完成。
- 分析脚本 `analysis.py` 用 managed venv 跑通，生成 `analysis/reading_dashboard.html`(5MB, 内嵌plotly.js, 离线可开) + 5 个 CSV。
- **踩坑**：`4NRRMG` 后台 pip 安装 jieba+plotly 实际失败（site-packages 为空），前台重装成功（jieba 0.42.1 / plotly 6.9.0）。教训：后台 pip 任务要验证 site-packages，不能只看任务结束。
- **踩坑2**：`add(table(...))` 把表格串当成 fig 参数导致 AttributeError；改为 `add(table_html=table(...))`。`go.Pie` 的 color 必须放 `marker=dict(colors=...)`，不能直接 `color=`。
- 看板含 19 个图、16 个 section（摘要+15分析）、左侧导航。
- **关键发现**：累计阅读 458.5h / 691 有效天；偏好分类(真实时长) 文学87.7h>科幻54.2h>历史41.7h；偏好时段"昼夜不倦"；参与人格 沉浸105/掠读96/高亮42/思考35；想法情感(1305条) 正面209/中性1036/负面60；进度=100仅6本，读完本均笔记136 vs 未读完24（读完才深读）。

## 看板美化重设计（design-engine 专家团 5 人流程）✅
- 用户嫌初版 UI 丑 → 切 DesignEngineTeam 专家团（画统筹主理），走 SOP：discovery-analyst→design-system-expert→prototype-builder→critique-reviewer→export-specialist（本机无预置 agent 文件，以 general-purpose + 成员 ID 名义调度，流程完整）。
- 用户选视觉方向：**暖中性·柔和卡片风**（米白底/暖灰字/陶土+芥末等低饱和暖强调，人文杂志感）。硬约束继承：避开纯黑、避开 AI 紫、无障碍(≥4.5:1)、单色强调。
- 设计令牌（已落地 analysis.py）：页面 `#FBF8F3`/卡片 `#FFFFFF`/侧栏 `#F5EFE6`/主文字 `#2B2724`(非纯黑)/次 `#6B6259`/弱 `#7A716A`(原#9A9189对比不达标已改)/强调 `#C2724B`；8 色低饱和暖色分类板 `#C2724B,#D9A441,#8A9A6B,#B5563F,#A8774E,#C98A4B,#7E9B8E,#B7AE9F`；热力图暖阶 `#F7F1E6→#9A5436`。
- 实施：`analysis.py` 新增 Plotly `WARM` 模板(go.layout.Template) + COLORWAY + WARM_FONT；`fig2html` 套 template；`<style>` 整体替换为暖中性 CSS（:root 变量、64px页头、KPI顶部强调细条、侧栏当前态左条、分区卡片左条、无斑马纹表格 hover）；全部 9 个冷色 hex 替换为暖色（**消除 AI 紫 #B279A2→#C98A4B**）；热力图改暖色阶、饼图接 colorway。
- 审查(严过审) PASS：5维均≥3；Anti-Slop 无致命（纯黑/AI紫/冷蓝仅出现在内联 Plotly.js 库默认样式，渲染数据无）。修复项：B5 标题错写"蓝=笔记数"→改为砖红/灰绿并调笔记线色；弱文字 #9A9189→#7A716A 过 4.5:1；D13/A3/B5/B6 相邻暖红撞色拉开（改 #7E9B8E/#8A9A6B）。
- 最终：`analysis/reading_dashboard.html` 19 图 / 16 section / 6 KPI / 冷色残留 0 / Plotly.js 内嵌可离线 / 4.9MB。样式改动**未触动任何数据计算与 CSV 生成**。
- 复用点：下次任何 plotly 看板都可用这套 WARM 模板 + 暖中性 CSS（存 analysis.py 顶部，可抽成 skill）。侧栏滚动联动 scrollspy 为可选增强，本次未做。

## 看板年报化修订（第二轮 design-engine 流程）✅
- 用户反馈：①词云之类要像「年度报告」；②布局有的挤在一起、有的没显示全。
- 新建团队 design-engine-report-rev，复用 5 人 SOP 重跑一轮。
- 诊断病灶（自读 analysis.py 定位）：C8 原是 40 词横向条形图（非真词云）；所有图 height 写死 420，A1(15类双柱)/A2(15作者)/C9b(25词) 超长条形图挤在 420px；表格 `max-height:340px` 滚动截断→"没显示全"。
- 实施（analysis.py）：① 加封面 Hero（44px大标题+48px陶土大数字4个+细分隔线）、5 章分隔条（壹..伍）、3 条金句 pull-quote（取自用户划线原文）；② C8 改真词云——`wordcloud` 库生成 900×500 RGBA PNG（字体 C:/Windows/Fonts/msyh.ttc，暖色板随机取色），base64 内嵌，下方附 Top15 词表；③ 长条形图高度动态化 `bar_h(n)=max(440,n*28+100)`（A1/A2/C9b/C10/D12 不再挤）；④ 表格 `.tbl` 改 `max-height:none` 自然展开。
- 依赖：venv 装 `wordcloud 1.9.6`（拉入 matplotlib/numpy/PIL）；中文字体用系统 msyh.ttc。
- 审查(严过审) PASS；打磨轮修 2 处：金句 pull-quote 原函数按长度取前3导致 2/3 同书《载脑体的救赎》且书名含 z-library/证书噪点 → 改为「每书取最长达标划线、整体排序取前3」并 `_clean_title` 清洗（去《》、嵌套括号、1lib/z-lib/.sk/.pdf 等下载站残留）；Hero 渐变最浅处 #F3E9DD→#F6ECE0 给陶土大字留对比余量。
- 最终：`analysis/reading_dashboard.html` 17 张真实 Plotly 图（plotly-graph-div=17，与代码中 17 个 add(fig) 吻合；grep 到的 18 次 Plotly.newPlot 中有 1 次是内联 Plotly.js 库本体对 newPlot 的引用，非多出的图）+ 1 词云 + 1 Hero + 5 章 + 3 金句；外链 0、冷色 0、Plotly 内嵌、5.07MB、CSV 完好。导出交付确认可离线。数据逻辑/CSV 未动。
- 注：交付达初报"Plotly.newPlot=14"为误报，已独立 grep 复核=18（含1库内引用），真实图表 17 张一张不少。C8 为词云图（非 Plotly）。

## 新方向：划线→名言警句小卡片 + 后续出版（规划中，未执行）
- 用户意图：把微信读书划线做成「名言警句」小卡片，并计划后续出版（book form）。当前仅规划，未动手。
- **第一关卡是版权不是设计**：纯摘抄合集商业出版 ≈ 超"适当引用"（《著作权法》第24条：指明作者/作品名 + 引用不构成实质/主要部分 + 不影响正常使用/不损权益），侵权风险高。两条合法路径：①只收**公版书**（作者去世>50年，如经典名著/老子庄子莎士比亚）；②做成带大量**本人原创解读/书评**的"读书笔记"，引文作点缀（transformative）。取得 278 本授权不现实。
- 规划四层：①内容层——复用 weread_notes_export.json，用 LLM 对每条划线打「金句度」（独立性/普适性/凝练度/警句感），去重+标记公版状态，产出金句库 JSON；②设计层——单卡结构(大衬线引言/破折号署名/书名小字/主题标签)，统一风 vs 按书分主题，可走 design-engine 出样张；③生产层——金句库→卡片HTML模板→无头浏览器截图PNG→拼装PDF/EPUB/长图，复用 analysis.py 暖中性基建；④出版层——公众号/小红书试水(低风险)→电子书自出版→实体书(自费/传统，编辑卡版权)→周边(明信片/日历)。
- 建议推进序：先建金句库+版权分级 → 出3-5张样卡定调性 → 公众号试水2-4周验证 → 据反馈定出版形态。
- 待用户定方向再动手；未触发任何 skill/agent 调度。

## 金句卡片方向：用户三决策（~15:12）
- ①版权：用户明确"不用管版权，我就自己用" → 个人自用，取消版权门槛；public_domain 字段保留但不 gate。若日后出版再捡起。
- ②书名清洗：用户嫌 （全集）（李现…影视原著）（果麦经典）（典藏纪念版） 等括号噪声。build_quote_lib.py 加 `clean_title()`：优先提《》内真书名，否则截到首个（([【。已重跑，验证 窄门/悟空传/载脑体的救赎/1984/沉思录 均干净。
- ③全库渲染：用户问"金句库能做出全部吗" → 能。样张只是定调性，定完批量渲染整库。"全部"=入选 351 条（quality filter）；原始 6099 中 1888 条低分词碎片未进库，用户可选是否连做。
- 已知脏值：author 字段有 "13203"（用户上传书），用户只嫌书名噪声未提作者；卡片"——13203"仍丑，标记待清。
- 现状：design-engine-quotecard 团队已建（discovery-analyst 已交付需求摘要；design-system-expert 正在定 3 套风格令牌：暖中性/古典衬线/杂志风）。下一步 prototype-builder 用令牌+clean title 渲染全 351 条为卡片（单 HTML + 3 风格切换），再 critique-reviewer→export-specialist。

## 金句卡片成品交付 ✅（~15:45）
- Phase B 全流程跑通（SOP 5 人）：prototype-builder 渲染 `quote_lib/cards.html`（351 张，3 风格切换 A 暖中性/B 古典衬线/C 杂志风，打印样式，离线自包含，159KB）；critique-reviewer 5 维 23/25 PASS（P0=0）；P1 脏作者「13203」等 19 张降级为「— 《书名》」已修；export-specialist 打包 `quote_lib/cards_package.zip`（535KB，含 cards.html+金句库.json+金句库_top60.md+design-tokens.md+使用说明.txt），离线校验通过、351 节点确认。
- 交付路径：`D:/workbuddy/微信读书/quote_lib/cards_package.zip`（及 cards.html 可双击预览）。
- 用户三决策已落地：①个人自用不卡版权；②书名清洗（clean_title 截括号/提《》）；③全库 351 条渲染。
- 可选后续（用户未要求）：task #8 公众号/小红书试水文案；若日后出版再捡版权（公版/本人解读双轨）。
- 团队 design-engine-quotecard 收尾中（shutdown + TeamDelete）。

## 金句卡片·自我检查优化轮 ✅（~16:30 收尾）
- 用户要求："再次自我检查，看看怎么优化卡片，比方字号、位置、高亮颜色等等"。主理人自读 cards.html CSS+样例，提 4 项优化，用户 AskUserQuestion 全选：
  ①工具栏随主题变色（C 暗夜改深色工具栏）；②破折号+书名染强调色（用户说的"高亮颜色"）；③放大屏幕预览字号+新增「大图」聚焦模式（网格 minmax 360→460，body.focus 单列 560px 居中、引文×1.18）；④排版统一（短引文垂直居中、pill 三主题统一绝对定位右上角、B 留白 11.11→10.37cqw）。
- 新建团队 design-engine-quotecard-62f3（旧 design-engine-quotecard 跨 2 天已被自动清）。走 SOP：prototype-builder 落地 4 项（新增 --bar-* 变量、351 处 <span class="dash"> 与 <span class="a-book">、grid/focusBtn、pill inset）；critique-reviewer 首检 REVISE 1 项（A/B pill inset 用 2.6cqw 未与 C 的 var(--pad) 统一）+1 重叠隐患（A/B 透明描边 pill 可能压长引文首行）。
- 二次修正（prototype-builder 续做）：A/B pill inset 改 var(--pad) 对齐 C；A/B pill 加 background:var(--bg) 遮罩 + z-index:2 根治透明压字。复查 critique-reviewer-2 PASS（REVISE 清零、重叠清零、351 卡/三主题/打印/离线全 ✅，5 维 21/25，P0=P1=0）。
- export-specialist 重打包 cards_package.zip（覆盖旧包，5 文件根级、CRC OK）。主理人补正 使用说明.txt（旧把 C 误写为"描边·纹样"，改为"杂志风·暗夜"；新增「强调色高亮」「大图聚焦模式」两节），手工重 zip 校验通过（zip 549353 B，cards.html 170063 B 与源逐字节一致）。
- 最终交付：`D:/workbuddy/微信读书/quote_lib/cards_package.zip`（cards.html 实测 166KB 非之前口播的 197KB，以实测为准）+ 配套 金句库.json / 金句库_top60.md / design-tokens.md / 使用说明.txt。
- 团队 design-engine-quotecard-62f3 已 TeamDelete 清理。

## 金句卡片·撤回「大图模式」（~16:21）
- 用户："我不要大图模式，之前的就行" → 撤回优化项③的「大图」聚焦模式（按钮 + body.focus 单列/放大 CSS + JS），网格默认宽度保留 460px（非用户反对项）；其余 ①②④ 保留。
- 顺手修隐藏 bug：原 `--q-scale` 只在 `body.focus` 定义 1.18，正常态无默认值 → 83–85 行 `calc(*var(--q-scale))` 正常态无效、引文字号塌回继承值(约16px)、三档字号区分失效（之前审查漏判）。在 :root 补 `--q-scale:1` 默认，正常态字号恢复正确档位。
- 同步删 使用说明.txt 的「大图聚焦模式」节；重打包 cards_package.zip（CRC OK，5 文件，focus 残留 0，351 卡/351 dash/351 a-book 全在位）。
